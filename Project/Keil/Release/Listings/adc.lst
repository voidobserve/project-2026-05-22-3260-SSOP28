C51 COMPILER V9.60.7.0   ADC                                                               06/03/2026 18:44:12 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE ADC
OBJECT MODULE PLACED IN .\Release\Objects\adc.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\Hardware\adc.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECTOR(0X000
                    -C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\User\lib;..\..\Hardware) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Relea
                    -se\Listings\adc.lst) OBJECT(.\Release\Objects\adc.obj)

line level    source

   1          #include "adc.h"
   2          
   3          #if (FUEL_CAPACITY_SCAN_ENABLE)
   4          
   5          volatile u16 adc_val; // adc值，0~4095
   6          // 控制切换adc通道的状态机：
   7          static volatile u8 adc_channel_status = ADC_CHANNEL_STATUS_NONE;
   8          
   9          // adc配置，使用adc时还需要切换到对应的引脚通道
  10          void adc_config(void)
  11          {
  12   1      #if FUEL_CAPACITY_SCAN_ENABLE
  13   1          // 检测油量的引脚：
  14   1          P0_MD0 |= GPIO_P01_MODE_SEL(0x3); // 模拟模式
  15   1      #endif
  16   1      
  17   1          ADC_CFG1 |= (0x0F << 3) | // ADC时钟分频为16分频，为系统时钟/16
  18   1                      (0x01 << 0);  // adc0中断使能
  19   1          ADC_CFG2 = 0xFF;          // 通道0采样时间配置为256个采样时钟周期
  20   1      
  21   1          ADC_ACON0 = ADC_CMP_EN(0x1) |  // 打开ADC中的CMP使能信号
  22   1                      ADC_BIAS_EN(0x1) | // 打开ADC偏置电流能使信号
  23   1                      ADC_BIAS_SEL(0x1); // 偏置电流选择：1x
  24   1      
  25   1          ADC_TRGS0 |= (0x07 << 4); // 通道 0DLY 的 ADC 时钟个数选择，配置为 4n+1，4 * 29 + 1
  26   1          ADC_CHS0 |= (0x01 << 6);  // 使能 通道 0DLY 功能
  27   1          __EnableIRQ(ADC_IRQn);    // 使能ADC中断
  28   1          IE_EA = 1;                // 使能总中断
  29   1      
  30   1          adc_channel_set(ADC_CHANNEL_FUEL);
  31   1      }
  32          
  33          // 设置adc通道
  34          void adc_channel_set(adc_channel_t adc_channel)
  35          {
  36   1          ADC_CHS0 &= ~((0x01 << 4) |
  37   1                        (0x01 << 3) |
  38   1                        (0x01 << 2) |
  39   1                        (0x01 << 1) |
  40   1                        (0x01 << 0)); // 清空选择的adc0通路
  41   1      
  42   1          switch (adc_channel)
  43   1          {
  44   2      
  45   2      #if FUEL_CAPACITY_SCAN_ENABLE
  46   2              // 检测油量
  47   2          case ADC_CHANNEL_FUEL:
  48   2              ADC_ACON1 &= ~((0x01 << 5) |       // 关闭ADC外部参考选择信号
  49   2                             (0x07 << 0));       // 清空ADC内部参考电压的选择配置
  50   2              ADC_ACON1 |= (0x01 << 6) |         // 使能ADC内部参考信号
  51   2                           (0x03 << 3) |         // 关闭测试信号
  52   2                           (0x01 << 0);          // 内部参考电压选择 2.0 V
  53   2              ADC_CHS0 |= ADC_ANALOG_CHAN(0x01); // P01通路
C51 COMPILER V9.60.7.0   ADC                                                               06/03/2026 18:44:12 PAGE 2   

  54   2              break;
  55   2      
  56   2      #endif
  57   2          }
  58   1      
  59   1          ADC_CFG0 |= ADC_CHAN0_EN(0x1) | // 使能通道0转换
  60   1                      ADC_EN(0x1);        // 使能A/D转换
  61   1          // 切换完通道后，需要等待ADC模块配置稳定，至少20us以上
  62   1      }
  63          
  64          #if 0
              // 获取adc值，存放到变量adc_val中(adc单次转换)
              u16 adc_single_convert(void)
              {
                  ADC_CFG0 |= ADC_CHAN0_TRG(0x1); // 触发ADC0转换
                  while (!(ADC_STA & ADC_CHAN0_DONE(0x1)))
                      ;                                           // 等待转换完成
                  ADC_STA = ADC_CHAN0_DONE(0x1);                  // 清除ADC0转换完成标志位
                  return ((ADC_DATAH0 << 4) | (ADC_DATAL0 >> 4)); // 读取ADC0的值
              }
              
              // adc采集+滤波
              u16 adc_getval(void)
              {
                  u16 adc_val_tmp = 0;     // 存放单次采集到的ad值
                  u32 adc_val_sum = 0;     // 存放所有采集到的ad值的累加
                  u16 get_adcmax = 0;      // 存放采集到的最大的ad值
                  u16 get_adcmin = 0xFFFF; // 存放采集到的最小的ad值(初始值为最大值)
                  u8 i = 0;
              
                  for (i = 0; i < 20; i++)
                  {
                      adc_val_tmp = adc_single_convert(); // 获取一次ad值
                      if (i < 2)
                          continue; // 丢弃前两次采样的
                      if (adc_val_tmp > get_adcmax)
                          get_adcmax = adc_val_tmp; // 更新当前采集到的最大值
                      if (adc_val_tmp < get_adcmin)
                          get_adcmin = adc_val_tmp; // 更新当前采集到的最小值
                      adc_val_sum += adc_val_tmp;
                  }
              
                  adc_val_sum -= get_adcmax;        // 去掉一个最大
                  adc_val_sum -= get_adcmin;        // 去掉一个最小
                  adc_val_tmp = (adc_val_sum >> 4); // 除以16，取平均值
              
                  return adc_val_tmp;
              }
              #endif
 103          
 104          // 由定时器调用，adc通道切换
 105          void adc_channel_switch_by_isr(void)
 106          {
 107   1          switch (adc_channel_status)
 108   1          {
 109   2          case ADC_CHANNEL_STATUS_NONE:
 110   2          case ADC_CHANNEL_STATUS_SEL_FUEL_END: 
 111   2              adc_channel_status = ADC_CHANNEL_STATUS_SEL_FUEL_BEGIN;
 112   2              break;
 113   2          case ADC_CHANNEL_STATUS_SEL_FUEL_BEGIN:
 114   2              ADC_CFG0 |= ADC_CHAN0_TRG(0x1); // 触发ADC0转换
 115   2              adc_channel_status = ADC_CHANNEL_STATUS_SEL_FUEL_END;
C51 COMPILER V9.60.7.0   ADC                                                               06/03/2026 18:44:12 PAGE 3   

 116   2              break;
 117   2      
 118   2          default:
 119   2              break;
 120   2          }
 121   1      }
 122          
 123          void ADC_IRQHandler(void) interrupt ADC_IRQn
 124          {
 125   1          u16 adc_val; // 由后续赋值
 126   1      
 127   1          // 进入中断设置IP，不可删除
 128   1          __IRQnIPnPush(ADC_IRQn);
 129   1      
 130   1          // ---------------- 用户函数处理 -------------------
 131   1          if (ADC_STA & ADC_CHAN0_DONE(0x01))
 132   1          {
 133   2              ADC_STA |= ADC_CHAN0_DONE(0x01);                 // 清除ADC0转换完成标志位
 134   2              adc_val = (ADC_DATAH0 << 4) | (ADC_DATAL0 >> 4); // 读取ADC0的值
 135   2      
 136   2              switch (adc_channel_status)
 137   2              {
 138   3                  // case ADC_CHANNEL_STATUS_SEL_BATTERY_END:
 139   3                  //     bat_adc_val_samples_update(adc_val);
 140   3                  //     break;
 141   3      
 142   3              case ADC_CHANNEL_STATUS_SEL_FUEL_END:
 143   3                  fuel_capacity_adc_val_samples_update(adc_val);
 144   3                  break;
 145   3              }
 146   2          }
 147   1      
 148   1          // 退出中断设置IP，不可删除
 149   1          __IRQnIPnPop(ADC_IRQn);
 150   1      }
 151          
 152          #endif


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    196    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =      3    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
